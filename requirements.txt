# The active project is in smart-home-nids/
# See smart-home-nids/requirements.txt for the real dependency list.
#
# This minimal set is only for running the archived legacy-prototype/ scripts:
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
streamlit>=1.30
plotly>=5.18
joblib>=1.3
