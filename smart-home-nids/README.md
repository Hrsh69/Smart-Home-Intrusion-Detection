# 🛡️ Smart Home NIDS — Network Intrusion Detection System

A **production-quality, machine-learning-based Network Intrusion Detection System** designed to detect malicious network traffic generated by IoT devices in smart home environments.

Built on the **CIC IoT-2023** dataset (Canadian Institute for Cybersecurity) with a **Random Forest classifier**, **Streamlit dashboard**, **SQLite logging**, **SHAP explainability**, and **multi-channel alerting**.

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Smart Home NIDS                              │
├──────────────┬──────────────┬──────────────┬────────────────────────┤
│   Data Layer │   ML Engine  │  App Layer   │      Dashboard         │
│              │              │              │                        │
│  CIC IoT-23 │ preprocess   │  database.py │  📊 Overview           │
│  CSV files   │ train_model  │  alerts.py   │  🔴 Live Monitor      │
│  processed/  │ predict.py   │  .env config │  📈 Analysis           │
│  SQLite DB   │ explainab.   │              │  🖥️ Devices            │
│              │              │              │  📋 Logs               │
│              │              │              │  ⚙️ Settings            │
└──────────────┴──────────────┴──────────────┴────────────────────────┘
```

## ✨ Features

### Machine Learning
- **Random Forest classifier** with balanced class weights for imbalanced data
- **9 attack categories**: BENIGN, DDoS, DoS, Mirai, Recon, Spoofing, BruteForce, WebAttack, Malware
- **18 network flow features** selected via Random Forest importance + Mutual Information
- **RobustScaler** for outlier-heavy traffic data
- **SHAP explainability** (TreeExplainer) for model transparency
- **Prediction confidence** scores with severity classification

### Dashboard
- 🌙 **Dark glassmorphism theme** with Inter font and gradient accents
- 📊 **Real-time KPI cards** — total flows, threats, devices, alerts
- 🎯 **Threat detection gauge** — visual severity indicator
- 📈 **Interactive Plotly charts** — pie, bar, line, heatmap, gauge
- 🔴 **Live monitoring** — CSV upload, manual input, traffic simulation
- 📋 **Searchable logs** — filter by type, severity, IP, date range
- 📥 **CSV/JSON export** — download detection history
- 🖥️ **Device risk scoring** — per-device threat assessment
- ⚡ **Auto-refresh** — configurable interval

### Alerting
- 🖥️ Desktop notifications (plyer)
- 📧 Email alerts (SMTP)
- 📱 Telegram webhook
- ⏱️ Rate limiting (prevent alert storms)
- 🎚️ Severity-based routing

### Infrastructure
- 🐳 Docker & Docker Compose support
- 🔄 GitHub Actions CI/CD
- 🗄️ SQLite persistence (detections, devices, alerts, statistics)
- 🧪 Comprehensive test suite (pytest)
- 📝 PEP 8 compliant, type-hinted, docstrings throughout

### Live Capture *(in progress — Phase 2–4)*
- 🕵️ **ARP spoof MITM engine** (`src/capture/arp_spoof.py`) — routes target devices' traffic through this machine for capture; mandatory `--targets` flag, atexit/signal ARP restore, `--dry-run` mode
- 📡 **Packet sniffer** (`src/capture/sniffer.py`) — Scapy BPF capture (IP/TCP/UDP/ICMP) → thread-safe queue
- 🔀 **Flow assembler** (`src/capture/flow_assembler.py`) — bidirectional 5-tuple flows with 120 s idle timeout (CICFlowMeter default), extracts all 18 training-schema features
- ⚙️ **Pipeline** (`src/capture/pipeline.py`) — orchestrates spoofer → sniffer → assembler → predictor → DB → alerts
- The Live Capture tab in the dashboard (`dashboard/monitoring.py`) is implemented; **not yet wired into `app.py`** pending Phase 5 hardening (CAP\_NET\_RAW capabilities, systemd service)

---

## 📁 Project Structure

```
smart-home-nids/
├── app.py                          # Streamlit entry point
├── config/
│   ├── config.py                   # Pipeline & hyperparameter config
│   ├── settings.py                 # Runtime settings (loaded from .env)
│   └── attack_mapping.py           # CIC IoT-2023 label → category mapping
├── src/
│   ├── predict.py                  # Prediction engine (18 features, 9 classes)
│   ├── explainability.py           # SHAP TreeExplainer integration
│   ├── database.py                 # SQLite persistence layer
│   ├── alerts.py                   # Multi-channel alerting
│   ├── preprocessing.py            # End-to-end data preprocessing pipeline
│   ├── train_model.py              # Model training + evaluation
│   ├── chunk_sampling.py           # Memory-efficient CIC data sampling
│   ├── cleaning.py                 # Data cleaning & validation
│   ├── encoding.py                 # Categorical encoding
│   ├── feature_engineering.py      # Feature creation
│   ├── feature_selection.py        # Feature selection (RF + MI)
│   ├── scaling.py                  # RobustScaler pipeline
│   ├── split_data.py               # Stratified train/test split
│   ├── utils.py                    # Utility functions
│   └── capture/                    # ⚠️ In-progress live capture module
│       ├── arp_spoof.py            #   ARP MITM engine
│       ├── sniffer.py              #   Scapy packet sniffer → queue
│       ├── flow_assembler.py       #   5-tuple flow tracker → 18 features
│       └── pipeline.py             #   Orchestration (spoof+sniff+predict+log)
├── dashboard/
│   ├── styles.py                   # Dark theme CSS + Plotly theming
│   ├── home.py                     # 📊 Overview page
│   ├── monitoring.py               # 🔴 Live Monitor (manual / simulate / capture)
│   ├── analysis.py                 # 📈 Attack Analysis page
│   ├── devices.py                  # 🖥️ Device Stats page
│   ├── logs.py                     # 📋 History & Logs page
│   └── settings_page.py            # ⚙️ Settings page
├── tests/
│   ├── test_predict.py             # Prediction engine tests
│   ├── test_database.py            # Database layer tests
│   ├── test_model.py               # Model validation tests
│   ├── test_alerts.py              # Alert system tests
│   ├── test_manual_input.py        # Manual prediction smoke-test script
│   └── test_simulation.py          # Simulation smoke-test script
├── data/
│   ├── raw/                        # Place CIC IoT-2023 CSVs here
│   └── processed/                  # Pipeline outputs
├── models/                         # Saved ML artifacts (.pkl)
├── reports/                        # JSON reports + HTML summary
├── plots/                          # PNG visualizations
├── Dockerfile                      # Docker build
├── docker-compose.yml              # Docker Compose
├── .github/workflows/ci.yml        # GitHub Actions CI
├── .env.example                    # Environment template
├── requirements.txt                # Python dependencies (pinned)
└── README.md                       # This file
```

---

## 🚀 Quick Start

### 1. Clone & Setup

```bash
git clone https://github.com/Hrsh69/Smart-Home-Intrusion-Detection.git
cd Smart-Home-Intrusion-Detection/smart-home-nids
python3 -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Prepare Data

Place CIC IoT-2023 CSV files in `data/raw/`, or the pipeline will auto-discover them from `../train/`, `../test/`, `../validation/`.

### 3. Run Preprocessing

```bash
python src/preprocessing.py
```

**Output**: `data/processed/processed_train.csv`, `data/processed/processed_test.csv`, model artifacts in `models/`.

### 4. Train Model

```bash
python src/train_model.py
```

**Output**: `models/rf_model.pkl`, `reports/training_report.json`, `plots/confusion_matrix.png`.

### 5. Launch Dashboard

```bash
streamlit run app.py
```

Open `http://localhost:8501` in your browser.

---

## 🐳 Docker

```bash
# Copy and configure environment
cp .env.example .env

# Build and run
docker-compose up -d

# Access at http://localhost:8501
```

---

## 🧪 Testing

```bash
python -m pytest tests/ -v
```

---

## 📊 Dataset

**CIC IoT-2023** — Canadian Institute for Cybersecurity

| Metric | Value |
|---|---|
| Raw rows scanned | 7,845,673 |
| Sampled rows | 156,254 |
| Clean rows | 155,261 |
| Train / Test split | 124,208 / 31,053 |
| Features selected | 18 |
| Attack categories | 9 |

### Class Distribution

| Class | Samples | Notes |
|---|---|---|
| BENIGN | 25,000 | ✅ Target reached |
| DDoS | 25,000 | ✅ Target reached |
| DoS | 25,000 | ✅ Target reached |
| Mirai | 25,000 | ✅ Target reached |
| Recon | 25,000 | ✅ Target reached |
| Spoofing | 25,000 | ✅ Target reached |
| BruteForce | 2,150 | ⚠️ Limited in source data |
| WebAttack | 3,530 | ⚠️ Limited in source data |
| Malware | 574 | ⚠️ Limited in source data |

### Selected Features

`iat`, `avg`, `header_length`, `header_bytes_per_packet`, `rst_count`, `tot_size`, `max`, `tot_sum`, `flow_duration`, `urg_count`, `rate`, `bytes_per_packet`, `variance`, `packets_per_second`, `protocol_type`, `min`, `rst_ratio`, `urg_ratio`

---

## ⚙️ Configuration

### Pipeline Config (`config/config.py`)
- Paths, random seed, feature selection parameters, scaler choice, split ratio

### Runtime Settings (`.env`)
- Alert channels (email, Telegram, desktop)
- Dashboard refresh interval and theme
- Model hyperparameters

---

## 🔔 Alert Channels

| Channel | Severity Trigger | Config |
|---|---|---|
| Desktop | Medium+ | Enabled by default |
| Email | High+ | Set SMTP vars in `.env` |
| Telegram | Critical | Set bot token in `.env` |

---

## 📚 Technology Stack

| Component | Technology |
|---|---|
| Language | Python 3.11+ |
| ML Framework | scikit-learn |
| Dashboard | Streamlit + Plotly |
| Database | SQLite |
| Explainability | SHAP |
| Notifications | plyer, SMTP, Telegram API |
| Containerization | Docker |
| CI/CD | GitHub Actions |
| Testing | pytest |

---
